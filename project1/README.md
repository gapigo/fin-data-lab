# Projeto 1

Estrutura inicial criada automaticamente.

Como usar:

- Navegue até `project1` e crie um ambiente virtual.
- Instale dependências (se houver) e rode o módulo principal.

Exemplo:

```powershell
cd project1
python -m src.project1.main
```
